export const HOME_PATH = '/';

export const SIGN_IN_PATH = '/sign-in';

export const SIGN_UP_PATH = '/sign-up';

export const PROFILE_PATH = '/profile';

export const careerStepEditPath = (stepId: string): string =>
  `/career-steps/${stepId}/edit`;
